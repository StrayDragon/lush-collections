# language: zh-CN

功能: 只读保护
  作为后端开发者
  我希望所有写入操作在只读会话中被拒绝

  背景:
    假设 数据库连接已就绪
    假设 使用 "标准CRUD" 作为当前 DAL

  场景: update_only_set 在只读会话被阻止
    假设 已存在一条名称为 "ro-create" 的记录
    当 尝试在只读会话中更新记录名称为 "ro-should-fail"
    那么 操作被阻止

  场景: update_full 在只读会话被阻止
    假设 已存在一条名称为 "ro-full" 的记录
    当 尝试在只读会话中全量更新
    那么 操作被阻止

  场景: update_partial 在只读会话被阻止
    假设 已存在一条名称为 "ro-partial" 的记录
    当 尝试在只读会话中部分更新
    那么 操作被阻止

  场景: delete 在只读会话被阻止
    假设 已存在一条名称为 "ro-delete" 的记录
    当 尝试在只读会话中删除
    那么 操作被阻止

  场景: batch_update 在只读会话被阻止
    假设 已存在一条名称为 "ro-batch" 的记录
    当 尝试在只读会话中批量更新
    那么 操作被阻止

  场景: optimistic_lock 在只读会话被阻止
    假设 已存在一条名称为 "ro-opt" 的记录
    当 尝试在只读会话中乐观锁更新
    那么 操作被阻止
