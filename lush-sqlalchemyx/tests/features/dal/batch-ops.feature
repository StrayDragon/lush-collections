# language: zh-CN

功能: 批量操作
  作为后端开发者
  我希望能批量查询、按字段查询、批量更新

  背景:
    假设 数据库连接已就绪

  场景: 批量按 ID 查询实体
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "B1" 和 "B2" 的两条记录
    当 批量按 ID 查询这两个实体
    那么 返回的结果不为空

  场景: 批量按 ID 查询空列表
    假设 使用 "简单CRUD" 作为当前 DAL
    当 批量按 ID 查询空列表
    那么 返回的结果为空

  场景: 批量按 ID 查询返回 DTO
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "B-DTO" 的记录
    当 批量按 ID 查询并返回 DTO
    那么 返回的结果不为空

  场景: 批量按 name 字段查询
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "field-lookup" 的记录
    当 批量按 name 字段查询 "field-lookup"
    那么 返回的结果不为空

  场景: 批量按 name 字段查询空列表
    假设 使用 "简单CRUD" 作为当前 DAL
    当 批量按 name 字段查询空列表
    那么 返回的结果为空

  场景: 批量按 ID 更新
    假设 使用 "标准CRUD" 作为当前 DAL
    并且 已存在一条名称为 "batch-upd" 的记录
    并且 将该记录的值设置为 "0"
    当 批量按 ID 更新值为 "88"
    那么 受影响行数为 1
    并且 数据库表中 ID 为当前实体 ID 的记录的 "value" 应为 "88"

  场景: 批量按 ID 更新空列表
    假设 使用 "标准CRUD" 作为当前 DAL
    当 批量按 ID 更新空列表
    那么 受影响行数为 0

  场景: 批量按条件更新名称
    假设 使用 "标准CRUD" 作为当前 DAL
    并且 已存在一条名称为 "cond-before" 的记录
    当 批量按当前实体 ID 条件更新名称为 "cond-after"
    那么 返回的实体名称应为 "cond-after"

  场景: 批量按条件更新值
    假设 使用 "标准CRUD" 作为当前 DAL
    并且 已存在一条名称为 "cond-val" 的记录
    并且 将该记录的值设置为 "0"
    当 批量按 ID 条件更新值为 "77"
    那么 数据库表中 ID 为当前实体 ID 的记录的 "value" 应为 "77"

  场景: 批量更新无效 key 抛出异常
    假设 使用 "标准CRUD" 作为当前 DAL
    并且 已存在一条名称为 "bad-key" 的记录
    当 批量按无效 key 更新
    那么 抛出了 ValueError
