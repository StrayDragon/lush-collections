# language: zh-CN

功能: 软删除行为
  作为后端开发者
  我希望删除操作不真正从数据库中移除记录
  以便支持数据恢复和审计追溯

  背景:
    假设 数据库连接已就绪
    假设 使用 "标准CRUD" 作为当前 DAL

  场景: 删除记录后查询返回空 (软删除可见性)
    假设 已存在一条名称为 "软删除测试" 的记录
    当 删除该记录
    那么 返回的结果为 True
    那么 删除后查询结果为空

  场景: 删除不存在的记录返回失败
    当 查询不存在的记录 ID "99999"
    那么 返回的结果为空

  场景: 更新被软删除的记录无副作用
    假设 已存在一条名称为 "更新旁观者" 的记录
    并且 已存在一条名称为 "待删除目标" 的记录
    当 统计记录总数
    那么 返回的记录总数至少为 2

  场景: 创建后立即删除返回一致结果
    假设 已存在一条名称为 "创建即删" 的记录
    当 删除该记录
    那么 返回的结果为 True
    那么 删除后查询结果为空
