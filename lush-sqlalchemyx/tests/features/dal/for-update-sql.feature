# language: zh-CN

功能: 悲观锁与 SQL 执行
  作为后端开发者
  我希望通过 SELECT ... FOR UPDATE 和裸 SQL 执行功能

  背景:
    假设 数据库连接已就绪
    假设 使用 "标准CRUD" 作为当前 DAL

  场景: 通过 ID 加悲观锁查询
    假设 已存在一条名称为 "lock-get" 的记录
    当 通过 ID 加悲观锁查询该记录
    那么 返回的实体不为空
    并且 返回的实体名称应为 "lock-get"

  场景: 批量加悲观锁查询
    假设 已存在一条名称为 "batch-lock1" 和 "batch-lock2" 的两条记录
    当 批量加悲观锁查询这两个 ID
    那么 返回的结果不为空

  场景: 批量加悲观锁查询空列表
    当 批量加悲观锁查询空列表
    那么 返回的结果为空

  场景: 按条件加悲观锁查询单条记录
    假设 已存在一条名称为 "one-lock" 的记录
    当 按条件加悲观锁查询单条记录
    那么 返回的实体不为空
    并且 返回的实体名称应为 "one-lock"

  场景: 执行裸 SQL
    当 执行裸 SQL: "SELECT 1"
    那么 返回的结果不为空
