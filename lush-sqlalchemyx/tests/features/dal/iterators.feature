# language: zh-CN

功能: 迭代器遍历和游标
  作为后端开发者
  我希望按批遍历全量/条件数据

  背景:
    假设 数据库连接已就绪
    假设 使用 "简单CRUD" 作为当前 DAL

  场景: 遍历全部记录
    假设 已存在一条名称为 "iter1" 的记录
    假设 已存在一条名称为 "iter2" 的记录
    当 迭代遍历全部记录
    那么 遍历结果至少有 2 条

  场景: 按条件遍历
    假设 已存在一条名称为 "where-iter" 的记录
    当 按名称 "where-iter" 条件迭代遍历
    那么 遍历结果至少有 1 条

  场景: 遍历返回 DTO
    假设 已存在一条名称为 "iter-dto" 的记录
    当 迭代遍历返回 DTO
    那么 遍历结果至少有 1 条
