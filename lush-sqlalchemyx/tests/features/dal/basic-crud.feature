# language: zh-CN

功能: 基础 CRUD 操作
  作为后端开发者
  我希望通过 DAL 层进行基本的增删改查操作
  以便快速构建数据持久化逻辑

  背景:
    假设 数据库连接已就绪

  # ── 创建 ──

  场景: 创建新记录并返回实体
    假设 使用 "简单CRUD" 作为当前 DAL
    当 创建一条名称为 "新纪录" 的新记录
    那么 返回的实体不为空
    并且 返回的实体名称应为 "新纪录"
    并且 返回的实体的 "id" 应为 "1"

  场景: 创建记录并返回 DTO
    假设 使用 "简单CRUD" 作为当前 DAL
    当 创建一条名称为 "DTO测试" 的记录并返回 DTO
    那么 返回的 DTO 名称应为 "DTO测试"

  # ── 查询 ──

  场景: 通过 ID 查询已存在的记录
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "查询测试" 的记录
    当 通过 ID 查询该记录
    那么 返回的实体不为空
    并且 返回的实体名称应为 "查询测试"

  场景: 通过 ID 查询不存在的记录
    假设 使用 "简单CRUD" 作为当前 DAL
    当 查询不存在的记录 ID "99999"
    那么 返回的结果为空

  场景: 检查记录是否存在
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "存在性测试" 的记录
    当 检查记录是否存在
    那么 返回的结果为 True

  # ── 更新 ──

  场景: 更新已存在的记录
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "更新前" 的记录
    当 使用新 CU 将记录名称更新为 "更新后"
    那么 返回的实体不为空
    并且 返回的实体名称应为 "更新后"

  # ── 删除 ──

  场景: 删除已存在的记录
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "待删除" 的记录
    当 删除该记录
    那么 返回的结果为 True
    那么 删除后查询结果为空

  # ── 统计 ──

  场景: 统计记录总数
    假设 使用 "简单CRUD" 作为当前 DAL
    并且 已存在一条名称为 "统计测试" 的记录
    当 统计记录总数
    那么 返回的记录总数至少为 1
