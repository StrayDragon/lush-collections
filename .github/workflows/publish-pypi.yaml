name: Publish to PyPI

on:
  push:
    tags:
      - "lush-*-v*"
  workflow_dispatch:
    inputs:
      package:
        description: "Package directory (e.g. lush-stdx)"
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    env:
      # Pin Docker images to avoid tag drift / surprise upgrades on CI.
      # Tests that need external services (e.g. lush-redisx / lush-sqlalchemyx)
      # will use these images via pytest fixtures.
      LUSH_TEST_REDIS_IMAGE: redis:7.4-alpine
      LUSH_TEST_MYSQL_IMAGE: mysql:8.0.40-debian
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.10"
          enable-cache: true

      - name: Resolve package
        id: resolve
        shell: bash
        run: |
          set -euo pipefail

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            pkg="${{ inputs.package }}"
            version=""
          else
            ref="${GITHUB_REF_NAME}"
            pkg="${ref%-v*}"
            version="${ref#${pkg}-v}"
          fi

          if [ -z "$pkg" ]; then
            echo "::error::Package is required."
            exit 2
          fi

          if [ ! -f "$pkg/pyproject.toml" ]; then
            echo "::error::pyproject.toml not found under: $pkg"
            exit 2
          fi

          echo "package=$pkg" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Sync (frozen)
        shell: bash
        run: |
          set -euo pipefail
          cd "${{ steps.resolve.outputs.package }}"
          uv sync -p 3.10 --frozen --default-index https://pypi.org/simple

      - name: Verify tag version matches pyproject
        if: ${{ steps.resolve.outputs.version != '' }}
        shell: bash
        run: |
          set -euo pipefail
          cd "${{ steps.resolve.outputs.package }}"
          expected="${{ steps.resolve.outputs.version }}"
          actual="$(uv version --short)"
          if [ "$actual" != "$expected" ]; then
            echo "::error::Tag version mismatch. tag=$expected pyproject=$actual"
            exit 2
          fi

      - name: Test
        shell: bash
        run: |
          set -euo pipefail
          cd "${{ steps.resolve.outputs.package }}"
          uv run -p 3.10 --default-index https://pypi.org/simple pytest

      - name: Build
        shell: bash
        run: |
          set -euo pipefail
          cd "${{ steps.resolve.outputs.package }}"
          uv build --default-index https://pypi.org/simple

      - name: Publish (Trusted Publisher)
        shell: bash
        run: |
          set -euo pipefail
          cd "${{ steps.resolve.outputs.package }}"
          uv publish --trusted-publishing=always
