# lush-logx — 子模块约定

> Logging helpers and a CLI log parser for structlog.

## Changelog

- **每次 minor/major (破坏性) 发布** 须在 `CHANGELOG.md` 中记录破坏性变更和重要变更.
- 记录内容: 破坏性变更 (Breaking Changes) + 重要变更 (Changes).
- 非破坏性修复 (patch) 不记录.

## 测试 & 覆盖率

- 100% branch coverage, `--cov-fail-under=100`.
